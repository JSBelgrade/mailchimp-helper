# mailchimp-helper
Handles subscription to MailChimp because their JS API sucks

## How to install

### Prerequests

1. io.js or node.js with `--harmony` flag

### Steps

1. Run `npm install`
2. Run `node index.js`
